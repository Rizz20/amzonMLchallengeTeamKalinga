# Core pipeline — all Python 3.14 compatible (no source compilation needed)
lightgbm==4.7.0
scikit-learn==1.9.1
rapidfuzz==3.14.6
jellyfish==1.2.1
datasketch==2.0.0
numpy==2.5.3
scipy==1.18.1
tqdm==4.70.1
joblib==1.6.0

# NOTE: pandas is intentionally excluded — our pipeline uses stdlib csv,
# which is faster for streaming large TSV files and has no build issues.

# Optional: multilingual embedding re-ranker (Apache 2.0, ~118M params)
# Uncomment if using the LLM re-ranker stage
# sentence-transformers==3.0.1
# torch==2.3.0
